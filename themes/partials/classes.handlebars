<h1>Classe {{name}}</h1>
<div class="box meta">
    {{#if uses}}
        <div class="uses">
            Usa
            <ul class="inline commas">
                {{#uses}}
                    <li><a href="{{.}}.html">{{.}}</a></li>
                {{/uses}}
            </ul>
        </div>
    {{/if}}

    {{#if extends}}
        <div class="extends">
            Estende {{#crossLink extends}}{{/crossLink}}
        </div>
    {{/if}}

    {{#if foundAt}}
        <div class="foundat">
            Definido em: <a href="{{foundAt}}">`{{{file}}}:{{{line}}}`</a>
        </div>
    {{/if}}

    {{#if module}}
        {{#if submodule}}
            Módulo: {{#crossLinkModule submodule}}{{/crossLinkModule}}<br>
            Módulo Pai: {{#crossLinkModule module}}{{/crossLinkModule}}
        {{else}}
            Módulo: {{#crossLinkModule module}}{{/crossLinkModule}}
        {{/if}}
    {{/if}}

    {{#if since}}
        <p>Disponível desde {{since}}</p>
    {{/if}}
</div>

{{#if deprecated}}
    <div class="box deprecated">
        <p>
        {{#if deprecationMessage}}
            <strong>Depreciado:</strong> {{deprecationMessage}}
        {{else}}
            Esta classe está depreciada.
        {{/if}}
        </p>
    </div>
{{/if}}

<div class="box intro">
    {{{classDescription}}}
</div>

{{#is_constructor}}
    <div class="constructor">
        <h2>Construtor</h2>
        {{>method}}
    </div>
{{/is_constructor}}

<div id="classdocs" class="tabview">
    <ul class="api-class-tabs">
        <li class="api-class-tab index"><a href="#index">Índice</a></li>

        {{#if methods}}
            <li class="api-class-tab methods"><a href="#methods">Métodos</a></li>
        {{/if}}
        {{#if properties}}
            <li class="api-class-tab properties"><a href="#properties">Propriedades</a></li>
        {{/if}}
        {{#if attrs}}
            <li class="api-class-tab attrs"><a href="#attrs">Atributos</a></li>
        {{/if}}
        {{#if events}}
            <li class="api-class-tab events"><a href="#events">Eventos</a></li>
        {{/if}}
    </ul>

    <div>
        <div id="index" class="api-class-tabpanel index">
            <h2 class="off-left">Índice de Itens</h2>

            {{#if methods}}
                <div class="index-section methods">
                    <h3>Métodos</h3>

                    <ul class="index-list methods{{#if extends}} extends{{/if}}">
                        {{#methods}}
                            <li class="index-item method{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                                <a href="#method_{{name}}">{{name}}</a>

                                {{#if static}}
                                    <span class="flag static">estático</span>
                                {{/if}}
                                {{#if deprecated}}
                                    <span class="flag deprecated">depreciado</span>
                                {{/if}}
                            </li>
                        {{/methods}}
                    </ul>
                </div>
            {{/if}}

            {{#if properties}}
                <div class="index-section properties">
                    <h3>Properties</h3>

                    <ul class="index-list properties{{#if extends}} extends{{/if}}">
                        {{#properties}}
                            <li class="index-item property{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                                <a href="#property_{{name}}">{{name}}</a>

                                {{#if static}}
                                    <span class="flag static">estático</span>
                                {{/if}}
                                {{#if deprecated}}
                                    <span class="flag deprecated">depreciado</span>
                                {{/if}}
                            </li>
                        {{/properties}}
                    </ul>
                </div>
            {{/if}}

            {{#if attrs}}
                <div class="index-section attrs">
                    <h3>Atributos</h3>

                    <ul class="index-list attrs{{#if extends}} extends{{/if}}">
                        {{#attrs}}
                            <li class="index-item attr{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                                <a href="#attr_{{name}}">{{name}}</a>
                            </li>
                        {{/attrs}}
                    </ul>
                </div>
            {{/if}}

            {{#if events}}
                <div class="index-section events">
                    <h3>Eventos</h3>

                    <ul class="index-list events{{#if extends}} extends{{/if}}">
                        {{#events}}
                            <li class="index-item event{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                                <a href="#event_{{name}}">{{name}}</a>

                                {{#if static}}
                                    <span class="flag static">estático</span>
                                {{/if}}
                                {{#if deprecated}}
                                    <span class="flag deprecated">depreciado</span>
                                {{/if}}
                            </li>
                        {{/events}}
                    </ul>
                </div>
            {{/if}}
        </div>

        {{#if methods}}
            <div id="methods" class="api-class-tabpanel">
                <h2 class="off-left">Métodos</h2>

                {{#methods}}
                    {{>method}}
                {{/methods}}
            </div>
        {{/if}}

        {{#if properties}}
            <div id="properties" class="api-class-tabpanel">
                <h2 class="off-left">Propriedades</h2>

                {{#properties}}
                    {{>props}}
                {{/properties}}
            </div>
        {{/if}}

        {{#if attrs}}
            <div id="attrs" class="api-class-tabpanel">
                <h2 class="off-left">Atributos</h2>

                {{#attrs}}
                    {{>attrs}}
                {{/attrs}}
            </div>
        {{/if}}

        {{#if events}}
            <div id="events" class="api-class-tabpanel">
                <h2 class="off-left">Eventos</h2>

                {{#events}}
                    {{>events}}
                {{/events}}
            </div>
        {{/if}}
    </div>
</div>
